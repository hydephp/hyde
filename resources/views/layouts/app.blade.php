<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name=generator content="HydePHP">

    <title>
        @if (isset($title))
        {{ config('hyde.name', 'HydePHP') }} - {{ $title }}
        @else
        {{ config('hyde.name', 'HydePHP') }}
        @endif
    </title>

    <!-- The compiled Tailwind styles generated by the app -->
    <link rel="stylesheet" href="{{ relative_path('media/tailwind.css', $currentPage) }}">
    
    <!-- The main stylesheet generated by the app -->
    <link rel="stylesheet" href="{{ relative_path('media/app.css', $currentPage) }}">

    @include('layouts.meta') 
</head>
<body id="app" class="flex flex-col min-h-screen overflow-x-hidden">
    @includeUnless($withoutNavigation ?? false, 'layouts.navigation') 

    <section id="content">
        @yield('content') 
    </section>

    @includeUnless(config('hyde.footer.enabled', true) && ($withoutNavigation ?? false), 'layouts.footer') 
</body>
</html>
